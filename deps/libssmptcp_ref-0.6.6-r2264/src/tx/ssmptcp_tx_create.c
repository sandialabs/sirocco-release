#include <ssmptcp_ref.h>
/*
ssmptcp_tx ssmptcp_tx_create(ssmptcp_tp tp, ssmptcp_addr addr, ssm_mr head, ssm_mr tail, ssm_md md, ssmptcp_call call)
{
  ssmptcp_tx result = SSMPTCP_NEW(tx);
  
  result->tp = tp;
  result->conn = ssmptcp_conn_find(tp, addr);
  result->tail = tail;
  result->head = head;
  result->md = md;
  result->rmd = NULL;
  result->call = call;
  return result;
}
*/
